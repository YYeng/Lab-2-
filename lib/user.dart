class User{
   String name, email, password,dateRegistered, rating, credit, status;

   User(
     {
       this.name,
       this.email,
       this.password,
       this.dateRegistered,
       this.rating,
       this.credit,
       this.status

     }
   );

}