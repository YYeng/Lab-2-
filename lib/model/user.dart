class User{
   String name, email, password,dateRegistered, rating, credit, status, phone, address;

   User(
     {
       this.name,
       this.email,
       this.password,
       this.dateRegistered,
       this.rating,
       this.credit,
       this.status,
       this.phone,
       this.address

     }
   );

}